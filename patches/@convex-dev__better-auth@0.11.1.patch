diff --git a/dist/client/adapter-utils.js b/dist/client/adapter-utils.js
index 1f804714d7bbfaf135fa64870842fd88e9002539..40cd2323cd71d5e3ce8dcdb39023d43dc9284ae4 100644
--- a/dist/client/adapter-utils.js
+++ b/dist/client/adapter-utils.js
@@ -8,6 +8,8 @@ export const adapterWhereValidator = v.object({
     operator: v.optional(v.union(v.literal("lt"), v.literal("lte"), v.literal("gt"), v.literal("gte"), v.literal("eq"), v.literal("in"), v.literal("not_in"), v.literal("ne"), v.literal("contains"), v.literal("starts_with"), v.literal("ends_with"))),
     value: v.union(v.string(), v.number(), v.boolean(), v.array(v.string()), v.array(v.number()), v.null()),
     connector: v.optional(v.union(v.literal("AND"), v.literal("OR"))),
+    // better-auth 1.6+: optional case-sensitivity for string matches (sensitive = default; insensitive = case-insensitive). Ignored by Convex for now (get-convex/better-auth#324).
+    mode: v.optional(v.union(v.literal("sensitive"), v.literal("insensitive"))),
 });
 export const adapterArgsValidator = v.object({
     model: v.string(),
diff --git a/dist/client/create-api.js b/dist/client/create-api.js
index d8faa6cf3ec63db230118c6eb97ccd21302d79a8..dab7ef00fa2e5b41cae5a73453f91f18d5784bea 100644
--- a/dist/client/create-api.js
+++ b/dist/client/create-api.js
@@ -9,6 +9,8 @@ const whereValidator = (schema, tableName) => v.object({
     operator: v.optional(v.union(v.literal("lt"), v.literal("lte"), v.literal("gt"), v.literal("gte"), v.literal("eq"), v.literal("in"), v.literal("not_in"), v.literal("ne"), v.literal("contains"), v.literal("starts_with"), v.literal("ends_with"))),
     value: v.union(v.string(), v.number(), v.boolean(), v.array(v.string()), v.array(v.number()), v.null()),
     connector: v.optional(v.union(v.literal("AND"), v.literal("OR"))),
+    // See adapterWhereValidator: same `mode` union as better-auth Where (packages/core/src/db/adapter/index.ts).
+    mode: v.optional(v.union(v.literal("sensitive"), v.literal("insensitive"))),
 });
 export const createApi = (schema, createAuthOptions) => {
     const betterAuthSchema = getAuthTables(createAuthOptions({}));
diff --git a/dist/plugins/convex/index.js b/dist/plugins/convex/index.js
index e0cfdf8f9f2c6b9fe4b06cfb85ba77790c87a4d8..ed994bb8c426a17fceba8cb09e7043d2afaf6db0 100644
--- a/dist/plugins/convex/index.js
+++ b/dist/plugins/convex/index.js
@@ -40,9 +40,12 @@ export const convex = (opts) => {
 export const convex = (opts) => {
     const jwtExpirationSeconds = opts.jwt?.expirationSeconds ?? opts.jwtExpirationSeconds ?? 60 * 15;
     const oidcProvider = oidcProviderPlugin({
+        // Convex reuses Better Auth's OIDC helper internally for JWKS/openid routes.
+        // Skip the upstream deprecation warning until convex-better-auth migrates.
+        __skipDeprecationWarning: true,
         loginPage: "/not-used",
         metadata: {
             issuer: `${process.env.CONVEX_SITE_URL}`,
             jwks_uri: `${process.env.CONVEX_SITE_URL}${opts.options?.basePath ?? "/api/auth"}/convex/jwks`,
         },
     });
diff --git a/src/plugins/convex/index.ts b/src/plugins/convex/index.ts
index 411017fac5f9f969ba342fe644eab6c8f6161e5f..5dd4fd15851745f7c9547ca960aaf0c8b0e85d73 100644
--- a/src/plugins/convex/index.ts
+++ b/src/plugins/convex/index.ts
@@ -178,10 +178,13 @@ export const convex = (opts: {
 }) => {
   const jwtExpirationSeconds =
     opts.jwt?.expirationSeconds ?? opts.jwtExpirationSeconds ?? 60 * 15;
   const oidcProvider = oidcProviderPlugin({
+    // Convex reuses Better Auth's OIDC helper internally for JWKS/openid routes.
+    // Skip the upstream deprecation warning until convex-better-auth migrates.
+    __skipDeprecationWarning: true,
     loginPage: "/not-used",
     metadata: {
       issuer: `${process.env.CONVEX_SITE_URL}`,
       jwks_uri: `${process.env.CONVEX_SITE_URL}${opts.options?.basePath ?? "/api/auth"}/convex/jwks`,
     },
   });
