/**
 * WorkOS AuthKit cookie and route constants.
 *
 * If AuthKit configuration ever changes the cookieName from the default,
 * update AUTHKIT_SESSION_COOKIE so sign-out cleanup stays in sync.
 */
export const AUTHKIT_SESSION_COOKIE = "wos-session" as const;
